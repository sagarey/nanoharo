---
phase: 02-container-layer-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agent-runner.ts
  - src/index.ts
  - src/ipc.ts
  - src/task-scheduler.ts
  - src/config.ts
  - src/container-runner.ts        # deleted
  - src/container-runtime.ts       # deleted
  - src/mount-security.ts          # deleted
  - src/container-runner.test.ts   # deleted
  - src/container-runtime.test.ts  # deleted
  - container/                     # deleted (entire directory)
autonomous: true
requirements:
  - RUNNER-02
  - RUNNER-03

must_haves:
  truths:
    - "src/container-runner.ts, src/container-runtime.ts, src/mount-security.ts 不存在"
    - "src/container-runner.test.ts, src/container-runtime.test.ts 不存在"
    - "container/ 目录不存在"
    - "npm run build 编译通过，零错误，零对已删除模块的引用"
    - "npm test 全部通过（group-queue.test.ts、db.test.ts 等存活测试不受影响）"
  artifacts:
    - path: "src/agent-runner.ts"
      provides: "writeGroupsSnapshot, writeTasksSnapshot, AvailableGroup（从 container-runner.ts 迁移过来）"
      exports: ["writeGroupsSnapshot", "writeTasksSnapshot", "AvailableGroup", "runInProcessAgent", "SDKSession", "MAIN_GROUP_FOLDER"]
    - path: "src/index.ts"
      provides: "import 路径指向 agent-runner.js"
      contains: "from './agent-runner.js'"
    - path: "src/ipc.ts"
      provides: "import 路径指向 agent-runner.js"
      contains: "from './agent-runner.js'"
    - path: "src/task-scheduler.ts"
      provides: "import 路径指向 agent-runner.js"
      contains: "from './agent-runner.js'"
  key_links:
    - from: "src/index.ts"
      to: "src/agent-runner.ts"
      via: "import { writeGroupsSnapshot, writeTasksSnapshot }"
      pattern: "from './agent-runner\\.js'"
    - from: "src/index.ts (line ~100)"
      to: "src/agent-runner.ts"
      via: "inline dynamic import type in return annotation"
      pattern: "import\\('./agent-runner\\.js'\\)\\.AvailableGroup"
    - from: "src/ipc.ts"
      to: "src/agent-runner.ts"
      via: "import { AvailableGroup }"
      pattern: "from './agent-runner\\.js'"
    - from: "src/task-scheduler.ts"
      to: "src/agent-runner.ts"
      via: "import { writeTasksSnapshot }"
      pattern: "from './agent-runner\\.js'"
---

<objective>
删除所有容器相关代码和文件，让代码库中不再有 Docker/容器运行时残留。

Purpose: Phase 1 已用 SDK in-process runner 替代容器调用，本阶段完成收尾——删除不再需要的文件、迁移仍在使用的三个符号、修复 import 路径、清理 config.ts 中的孤立常量。

Output:
- src/agent-runner.ts 新增 writeGroupsSnapshot、writeTasksSnapshot、AvailableGroup
- src/index.ts、src/ipc.ts、src/task-scheduler.ts import 路径从 container-runner.js 改为 agent-runner.js
- 5 个 src/ 文件被删除，container/ 目录被删除
- config.ts 中 4 个孤立的容器常量被移除
- npm run build 和 npm test 全部通过
</objective>

<execution_context>
@/workspaces/nanoharo/.claude/get-shit-done/workflows/execute-plan.md
@/workspaces/nanoharo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/workspaces/nanoharo/.planning/PROJECT.md
@/workspaces/nanoharo/.planning/ROADMAP.md
@/workspaces/nanoharo/.planning/STATE.md

<interfaces>
<!-- Key interfaces needed by the executor. Extracted from current codebase. -->

From src/agent-runner.ts (current top-level imports — must ADD fs and resolveGroupIpcPath):
```typescript
import path from 'path';
import fs from 'fs';  // MUST ADD — missing from current file, needed for snapshot functions
import { resolveGroupIpcPath } from './group-folder.js';  // MUST ADD — needed for snapshot functions

import { unstable_v2_createSession, unstable_v2_resumeSession, type SDKSession, type SDKSessionOptions } from '@anthropic-ai/claude-agent-sdk';
import { DATA_DIR, GROUPS_DIR, MAIN_GROUP_FOLDER } from './config.js';
import { readEnvFile } from './env.js';
import { logger } from './logger.js';
import { type RegisteredGroup } from './types.js';
```

From src/container-runner.ts (symbols to MIGRATE to agent-runner.ts):
```typescript
export interface AvailableGroup {
  jid: string;
  name: string;
  lastActivity: string;
  isRegistered: boolean;
}

export function writeTasksSnapshot(
  groupFolder: string,
  isMain: boolean,
  tasks: Array<{
    id: string;
    groupFolder: string;
    prompt: string;
    schedule_type: string;
    schedule_value: string;
    status: string;
    next_run: string | null;
  }>,
): void { /* writes to groupIpcDir/current_tasks.json */ }

export function writeGroupsSnapshot(
  groupFolder: string,
  isMain: boolean,
  groups: AvailableGroup[],
  registeredJids: Set<string>,
): void { /* writes to groupIpcDir/available_groups.json */ }
```

Exact import lines to CHANGE (from RESEARCH.md):
```
src/index.ts line 13:  from './container-runner.js'  →  from './agent-runner.js'
src/index.ts line ~100: import('./container-runner.js').AvailableGroup[]  →  import('./agent-runner.js').AvailableGroup[]
src/ipc.ts line 12:    from './container-runner.js'  →  from './agent-runner.js'
src/task-scheduler.ts line 12: from './container-runner.js'  →  from './agent-runner.js'
```

Orphaned constants in config.ts to REMOVE (safe once container-runner.ts deleted):
```
MOUNT_ALLOWLIST_PATH   — only used by container-runner.ts
CONTAINER_IMAGE        — only used by container-runner.ts
CONTAINER_TIMEOUT      — only used by container-runner.ts
CONTAINER_MAX_OUTPUT_SIZE — only used by container-runner.ts
```
DO NOT remove MAX_CONCURRENT_CONTAINERS — still used by group-queue.ts.
Also update config.ts line 8 comment: replace "container-runner.ts" with "agent-runner.ts".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 迁移 survivor 符号到 agent-runner.ts，修复 import 路径，删除 src 容器文件</name>
  <files>src/agent-runner.ts, src/index.ts, src/ipc.ts, src/task-scheduler.ts, src/container-runner.ts, src/container-runtime.ts, src/mount-security.ts, src/container-runner.test.ts, src/container-runtime.test.ts</files>
  <action>
**步骤必须严格按顺序执行，先迁移后删除，避免中间态编译失败。**

1. **在 src/agent-runner.ts 末尾追加 snapshot helpers 区块：**
   - 在文件顶部 import 区域新增两行：`import fs from 'fs';` 和 `import { resolveGroupIpcPath } from './group-folder.js';`
   - 在文件末尾（最后一行 `export { MAIN_GROUP_FOLDER };` 之后）追加：
     ```typescript
     // --- Snapshot helpers (migrated from container-runner.ts) ---

     export interface AvailableGroup {
       jid: string;
       name: string;
       lastActivity: string;
       isRegistered: boolean;
     }

     export function writeTasksSnapshot(
       groupFolder: string,
       isMain: boolean,
       tasks: Array<{
         id: string;
         groupFolder: string;
         prompt: string;
         schedule_type: string;
         schedule_value: string;
         status: string;
         next_run: string | null;
       }>,
     ): void {
       const groupIpcDir = resolveGroupIpcPath(groupFolder);
       fs.mkdirSync(groupIpcDir, { recursive: true });
       const filteredTasks = isMain
         ? tasks
         : tasks.filter((t) => t.groupFolder === groupFolder);
       const tasksFile = path.join(groupIpcDir, 'current_tasks.json');
       fs.writeFileSync(tasksFile, JSON.stringify(filteredTasks, null, 2));
     }

     export function writeGroupsSnapshot(
       groupFolder: string,
       isMain: boolean,
       groups: AvailableGroup[],
       _registeredJids: Set<string>,
     ): void {
       const groupIpcDir = resolveGroupIpcPath(groupFolder);
       fs.mkdirSync(groupIpcDir, { recursive: true });
       const visibleGroups = isMain ? groups : [];
       const groupsFile = path.join(groupIpcDir, 'available_groups.json');
       fs.writeFileSync(
         groupsFile,
         JSON.stringify({ groups: visibleGroups, lastSync: new Date().toISOString() }, null, 2),
       );
     }
     ```
   - 注意：`path` 已在文件中 import，只需加 `fs` 和 `resolveGroupIpcPath`。

2. **修复 src/index.ts 的两处引用：**
   - Line 13: `from './container-runner.js'` → `from './agent-runner.js'`
   - Line ~100 (getAvailableGroups return type): `import('./container-runner.js').AvailableGroup[]` → `import('./agent-runner.js').AvailableGroup[]`
   - **关键 pitfall：** 第二处在函数返回类型注解中，是 inline dynamic import，grep `from './container-runner'` 找不到，必须搜索 `container-runner` 字符串本身。

3. **修复 src/ipc.ts：**
   - Line 12: `from './container-runner.js'` → `from './agent-runner.js'`

4. **修复 src/task-scheduler.ts：**
   - Line 12: `from './container-runner.js'` → `from './agent-runner.js'`

5. **删除 5 个 src/ 文件（此时 import 路径已全部修好，删除安全）：**
   ```bash
   rm /workspaces/nanoharo/src/container-runner.ts
   rm /workspaces/nanoharo/src/container-runtime.ts
   rm /workspaces/nanoharo/src/mount-security.ts
   rm /workspaces/nanoharo/src/container-runner.test.ts
   rm /workspaces/nanoharo/src/container-runtime.test.ts
   ```

6. **快速验证：** 运行 `npm run build`（在 /workspaces/nanoharo 目录）确保编译通过后再进行 Task 2。
  </action>
  <verify>
    <automated>cd /workspaces/nanoharo && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - npm run build 输出零 error（允许 warning）
    - src/ 中不存在 container-runner.ts、container-runtime.ts、mount-security.ts、container-runner.test.ts、container-runtime.test.ts
    - grep -r "container-runner" src/ 返回空（无残留引用）
  </done>
</task>

<task type="auto">
  <name>Task 2: 删除 container/ 目录，清理 config.ts 孤立常量</name>
  <files>container/, src/config.ts</files>
  <action>
1. **删除 container/ 整个目录：**
   ```bash
   rm -rf /workspaces/nanoharo/container/
   ```
   包含 Dockerfile、agent-runner/、skills/（含 agent-browser）、build.sh 全部删除。
   不迁移任何内容——这些均为容器运行时文件，Phase 3 按需重新创建主 Dockerfile。

2. **清理 src/config.ts 孤立常量：**
   删除以下 4 个常量的完整声明块（含注释）：
   - `MOUNT_ALLOWLIST_PATH`
   - `CONTAINER_IMAGE`
   - `CONTAINER_TIMEOUT`
   - `CONTAINER_MAX_OUTPUT_SIZE`

   **保留** `MAX_CONCURRENT_CONTAINERS`——仍被 src/group-queue.ts 使用。

3. **更新 config.ts 顶部注释（line 8）：**
   将 `container-runner.ts` 替换为 `agent-runner.ts`，使注释与现实一致。

4. **最终验证：**
   - `npm run build` 编译通过
   - `npm test` 存活测试全部通过（group-queue.test.ts、db.test.ts、formatting.test.ts、group-folder.test.ts 等）
  </action>
  <verify>
    <automated>cd /workspaces/nanoharo && npm run build && npm test 2>&1 | tail -20</automated>
  </verify>
  <done>
    - container/ 目录不存在
    - config.ts 不包含 MOUNT_ALLOWLIST_PATH、CONTAINER_IMAGE、CONTAINER_TIMEOUT、CONTAINER_MAX_OUTPUT_SIZE
    - config.ts 仍包含 MAX_CONCURRENT_CONTAINERS
    - npm run build 零 error
    - npm test 全部通过
    - grep -r "container-runner\|container-runtime\|mount-security" src/ dist/ 返回空
  </done>
</task>

</tasks>

<verification>
## Phase 2 Complete Verification

```bash
# 1. 所有目标文件不存在
ls /workspaces/nanoharo/src/container-runner.ts 2>&1    # expected: No such file
ls /workspaces/nanoharo/src/container-runtime.ts 2>&1   # expected: No such file
ls /workspaces/nanoharo/src/mount-security.ts 2>&1      # expected: No such file
ls /workspaces/nanoharo/container/ 2>&1                  # expected: No such file

# 2. 无残留引用
grep -r "container-runner\|container-runtime\|mount-security" /workspaces/nanoharo/src/ /workspaces/nanoharo/dist/ 2>/dev/null
# expected: empty output

# 3. TypeScript 编译通过
cd /workspaces/nanoharo && npm run build
# expected: exit 0, zero errors

# 4. 存活测试通过
cd /workspaces/nanoharo && npm test
# expected: all passing

# 5. survivor 符号正确导出
grep -n "AvailableGroup\|writeGroupsSnapshot\|writeTasksSnapshot" /workspaces/nanoharo/src/agent-runner.ts
# expected: interface and function definitions present
```
</verification>

<success_criteria>
- src/container-runner.ts、src/container-runtime.ts、src/mount-security.ts 不存在
- src/container-runner.test.ts、src/container-runtime.test.ts 不存在
- container/ 目录不存在
- src/agent-runner.ts 导出 AvailableGroup、writeGroupsSnapshot、writeTasksSnapshot
- src/index.ts、src/ipc.ts、src/task-scheduler.ts 全部从 './agent-runner.js' 而非 './container-runner.js' 引入
- npm run build 编译通过，零错误
- npm test 全部通过
</success_criteria>

<output>
完成后创建 `/workspaces/nanoharo/.planning/phases/02-container-layer-removal/02-01-SUMMARY.md`
</output>
